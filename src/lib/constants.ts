// The production URL where users access the app
export const SITE_URL = "https://skctskilltracker.vercel.app";

// Helper to get current origin (works on any deployment)
export const getCurrentOrigin = () => window.location.origin;
